<adonisControls:AdonisWindow x:Class="DMMDZZ_Game_Start.Others.ChoosePathFind"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DMMDZZ_Game_Start.Others"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
        xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        mc:Ignorable="d"
        Title="选择游戏路径"
        Height="400"
        Width="600"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource MiSansFont}"
        >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock 
            Text="检测到多个游戏路径，请选择一个：" 
            FontSize="14" 
            FontWeight="Medium" 
            Margin="0,0,0,10" 
            VerticalAlignment="Center"
            />
        
        <ScrollViewer 
            Grid.Row="1" 
            Margin="0,10,0,10"
            >
            <ListBox 
                x:Name="lstPaths" 
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch"
                SelectionMode="Single"
                MouseDoubleClick="LstPaths_MouseDoubleClick"
                >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" ToolTip="双击选择"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
        
        <StackPanel 
            Grid.Row="2" 
            Orientation="Horizontal" 
            HorizontalAlignment="Right" 
            Margin="0,10,0,0">
            <Button 
                x:Name="BtnSelect" 
                Content="选择" 
                Width="80" 
                Height="28" 
                Margin="0,0,10,0" 
                Click="BtnSelect_Click"
                />
            <Button 
                x:Name="BtnCancel" 
                Content="取消" 
                Width="80" 
                Height="28" 
                Click="BtnCancel_Click" 
                />
        </StackPanel>
    </Grid>
</adonisControls:AdonisWindow>